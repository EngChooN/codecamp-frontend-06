import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 100px;
  background-color: yellow;
  font-size: 20px;
`;

export default function LayoutBanner() {
  return <Wrapper>여기는 배너영역</Wrapper>;
}
